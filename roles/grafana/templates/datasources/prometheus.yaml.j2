apiVersion: 1
datasources:
- name: Prometheus
  type: prometheus
  access: proxy
  orgId: 1
  url: http://localhost:9090
  # TODO: this assumes that grafana and prometheus run on same server.
  # ideally this should be decoupled with a secured connection
  version: 1
  editable: false
  jsonData:
    timeInterval: 1s
